sudo: true

matrix:
  include:
    - os: osx
      osx_image: xcode10
  fast_finish: true

branches:
  only:
    - master

install:
  - set -e && ./installer.sh

script:
  - set -e && ./cleanup.sh

notifications:
  email: false